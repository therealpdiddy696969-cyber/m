{
  "name": "smb-web",
  "private": true,
  "dependencies": {
    "fflate": "^0.8.2",
    "gl-matrix": "^3.4.3"
  },
  "devDependencies": {
    "esbuild": "^0.25.10",
    "playwright": "^1.44.0",
    "wrangler": "^4.65.0"
  },
  "scripts": {
    "build": "node build_web.mjs",
    "build:watch": "node build_web.mjs --watch",
    "test:e2e": "node tests/e2e.mjs"
  }
}
